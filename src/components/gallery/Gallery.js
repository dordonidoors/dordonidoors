import React from 'react'
import PropTypes from 'prop-types'

const Gallery = (props) => {
  window.scrollTo(0,0);
  return (
    <div style={{height: '100vh'}}>
      <h1>Gallery</h1>
    </div>
  )
}

export default Gallery
